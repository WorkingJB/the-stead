# The Stead — copy to .env.local and fill in. Use ONLY public client keys here.
# The linked project is "TheStead" (ref iaveyizkpqbmkhlsdlte). Confirm the ref
# matches before pointing at any cloud project — do not cross-wire another project.

NEXT_PUBLIC_SUPABASE_URL=https://<your-project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<your-anon-key>

NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Server-only secrets (service_role, Stripe, etc.) belong in deployment env vars,
# NOT in this file and NOT prefixed NEXT_PUBLIC_.
